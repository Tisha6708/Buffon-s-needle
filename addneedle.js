function addNeedles() {
    // Get the number of needles to throw from the input field
    const inputField = document.getElementById("needles_to_throw");
    const numNeedles = Number(inputField.value);

    // Your code to add the specified number of needles to the game goes here
    console.log("Throwing " + numNeedles + " needles");
}
